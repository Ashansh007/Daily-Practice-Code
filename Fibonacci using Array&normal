input=prompt("Enter the number:")
input=Number(input);
sum=0;
for(i=0;i<=input;i++)
{
    sum+=i;
console.log(sum)
}

*******************************************************
input=prompt("Enter the number:")
input=Number(input);
arr=[];
sum=0;
for(i=0;i<=input;i++){
    sum+=i;
    arr.push(sum);
}
console.log(arr)

